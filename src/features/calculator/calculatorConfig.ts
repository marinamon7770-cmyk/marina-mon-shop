/**
 * Настройки клиентского калькулятора стоимости плетёных изделий.
 * Все цифры для расчёта — только здесь. Меняйте значения и сохраняйте файл.
 */

/** Ссылка на заказ / вопрос мастеру (VK, Senler и т.п.). Пока пусто — кнопка скрыта. */
export const vkOrderUrl = "";

// ─── Общие параметры трубочек и стоек ───────────────────────────────────────

/** Длина одной трубочки, см */
export const tubeLength = 30;

/** Расстояние между стойками по периметру, см */
export const stakeDistance = 2.5;

/** Запас трубочек, доля (0 = 0%, 0.05 = 5%) */
export const reservePercent = 0;

/** Дополнительные трубочки в углах прямоугольника и квадрата */
export const cornerTubes = 2;

/** Направление стоек дна прямоугольника: по длинной стороне (используется в формулах) */
export const rectangleStakeDirection: "long" | "short" = "long";

// ─── Кратность стоек по форме изделия ───────────────────────────────────────

export const stakeMultiplicity = {
  /** Круглая корзина */
  circle: { bottom: 1, wall: 1, bend: 1 },
  /** Овальная корзина */
  oval: { bottom: 1, wall: 1, bend: 1 },
  /** Прямоугольный короб */
  rectangle: { bottom: 2, wall: 2, bend: 1 },
  /** Квадратный короб */
  square: { bottom: 1, wall: 1, bend: 1 },
} as const;

// ─── Стоимость материалов и работы ───────────────────────────────────────────

/** Расход на одну трубочку, ₽ */
export const materialCostPerTube = 1.625;

/** Желаемая зарплата мастера в месяц, ₽ */
export const monthlySalary = 30_000;

/** Рабочих часов в месяц */
export const monthlyHours = 176;

/** Наценка, доля (0.4 = 40%) */
export const markup = 0.4;

/** Коэффициент сложности для клиентского расчёта */
export const normalComplexityFactor = 1.2;

/** Стоимость фанерного дна, ₽ (добавляется только при выборе «фанерное дно») */
export const plywoodCost = 0;

// ─── Правила времени ─────────────────────────────────────────────────────────

/** Минимум часов на изделие */
export const baseHours = 4;

/** До этого периметра (см) дополнительные часы не добавляются */
export const timePerimeterIncluded = 150;

/** Шаг периметра для доп. часов, см */
export const timePerimeterStep = 100;

/** До этой высоты (см) дополнительные часы не добавляются */
export const timeHeightIncluded = 10;

/** Шаг высоты для доп. часов, см */
export const timeHeightStep = 20;

// ─── Правила упаковки ────────────────────────────────────────────────────────

/** Базовая стоимость упаковки (бирка + пакет + упаковка), ₽ */
export const basePackagingPrice = 100;

/** До этого периметра (см) доп. упаковка не добавляется */
export const packagingIncludedPerimeter = 150;

/** Шаг периметра для доп. упаковки, см */
export const packagingStep = 75;

/** Доплата за каждый шаг периметра сверх лимита, ₽ */
export const packagingStepPrice = 100;

// ─── Коэффициенты узоров (расход трубочек на плетение) ───────────────────────

export const patterns = {
  /** Узор «ситец» */
  sitc: {
    /** Дно (круг, овал, прямоугольник): числитель / знаменатель */
    bottom: 89 / 625,
    /** Стенки */
    wall: 71 / 500,
    /** Дно квадрата */
    squareBottom: 71 / 500,
  },
  /** Узор «верёвочка» — для дна и стенок */
  rope: {
    bottomAndWall: 3 / 14,
  },
} as const;
